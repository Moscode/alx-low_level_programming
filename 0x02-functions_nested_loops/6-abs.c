#include "main.h"

/**
 * _abs - It covert number to positive
 *
 * @n: number to be made psitive if needed
 *
 * Return: Always >0 (Success)
 */
int _abs(int n)
{
	if (n < 0)

		return (-1 * n);

	return (n);

}
